• The opt-in Tor connection is removed. Winnow now always connects to the Bitcoin network directly; if you had Tor on, the app tells you once at launch.
• Fewer moving parts: the embedded Tor client and its several hundred third-party components are gone, leaving one pinned cryptography library.
• Peer-list and explorer messages say plainly what each service learns.
